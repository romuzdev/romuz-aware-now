/**
 * M14 - Unified Dashboard Page (Updated)
 * Wrapper for the new UnifiedDashboard component
 */
import UnifiedDashboard from './UnifiedDashboard';

export default function UnifiedDashboardPage() {
  return <UnifiedDashboard />;
}
